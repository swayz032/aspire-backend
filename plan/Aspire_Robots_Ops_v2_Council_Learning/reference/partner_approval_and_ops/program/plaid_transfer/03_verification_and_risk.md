# Verification + Risk Controls

Include in application narrative:
- KYB/KYC steps before enabling transfers
- limits policy (initial caps + ramp)
- fraud/returns monitoring
- escalation policy: approval-only -> disabled

Operational rule:
- call authorization endpoint before create; deny/escalate on declined authorization
