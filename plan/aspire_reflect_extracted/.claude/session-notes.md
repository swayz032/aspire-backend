# Session Notes (for reflect)

- Correction: Always include correlationId in every log line and receipt.
- Pattern: For forms, validate input at the boundary and show explicit error state.
- Approval: Receipt schema additions must be backward-compatible.
- Never: Log raw PII or secrets in debug output.
